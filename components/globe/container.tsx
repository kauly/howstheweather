import { ReactNode } from "react";

type GlobeContainerProps = {
  children: ReactNode;
};

function GlobeContainer({ children }: GlobeContainerProps) {
  return <div></div>;
}

export { GlobeContainer };
